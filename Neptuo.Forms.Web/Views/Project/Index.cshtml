@model IEnumerable<Neptuo.Forms.Web.Models.ListProjectModel>

@{
    ViewBag.Title = "Index";
}

<div class="folder">
    <h2 class="folder-heading">@Loc("Your projects")</h2>

    @{
        if (Model.Count() > 0)
        {
            UI.Grid(Model)
                .AddColumn((L)"ID", p => p.ID, "w30")
                .AddColumn((L)"Name", p => p.Name)
                .AddColumn((L)"Created", p => p.Created, "w130")
                .AddColumn("", p => Html.ActionLink((L)"Edit", "edit", new { id = p.ID }) + " " + Html.ActionLink((L)"Forms", "forms", new { id = p.ID }), "w100")
                .Render();
        }
        else
        { 
            @Loc("No projects")
        }
    }

    <div class="folder-foot">
        @Html.ActionLink((L)"+Create", "create")
    </div>
</div>
